# Serveur principal (par défaut)
server {
    listen 8080;
    # host 127.0.0.1;
    # server_name localhost;
    # 
    # # Page d'erreur 404
    # error_page 505 404 /404.html;
    # # 
    # # # Limite taille requête à 1MB
    # clientMaxBodySize 1048576;
    # # 
    # # # Route principale
    location / {
        root ./www;
        index index.html;
        allowedMethods GET POST;
        autoindex on;
    }
    # # 
    # # # Route pour upload
    # location /upload {
    #     root ./www/upload;
    #     allowedMethods GET POST DELETE;
    #     uploadStore ./www/upload/files/;
    #     uploadEnabled on;
    # }
}


# Serveur secondaire
server{
	listen 8181;
}
#
# server{
# 	listen 9090;
# }
